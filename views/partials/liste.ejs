<div class="jumbotron">
    <table class="table">
        <thead>
            <tr>
                <th>#</th>
                <th>Date de création</th>
                <th>Nom</th>
                <th>Prix</th>
            </tr>
        </thead>
        <tbody>
            <% items.items.forEach((item, index)=> { %>
                <tr>
                    <td>
                        <%= index %>
                    </td>
                    <td>
                        <%= item.getDate() %>
                    </td>
                    <td>
                        <%= item.getName() %>
                    </td>
                    <td>
                        <%= item.getPrice() %>$
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</div>